<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="#1D6576ED" />

    <!-- Border stroke -->
    <stroke android:width="5dp" android:color="@color/light_grey" />

    <!-- Rounded corners -->
    <corners android:radius="10dp" />
</shape>
